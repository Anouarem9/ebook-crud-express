<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= ebook.titre %></title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f9;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      .ebook-container {
        background-color: #ffffff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        max-width: 500px;
        width: 100%;
        text-align: center;
      }

      h1 {
        color: #333;
        margin-bottom: 10px;
      }

      p {
        font-size: 16px;
        color: #555;
        margin: 8px 0;
      }

      strong {
        color: #222;
      }

      img {
        width: 100%;
        max-width: 250px;
        height: auto;
        border-radius: 8px;
        margin: 15px 0;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }

      .back-link,
      .edit-link {
        display: inline-block;
        border: none;
        margin-top: 15px;
        padding: 10px 15px;
        background-color: #5b5b5b;
        color: white;
        text-decoration: none;
        border-radius: 4px;
        font-size: 16px;
        transition: background 0.3s;
        min-width: 15%;
      }

      .back-link:hover {
        background-color: #4d4d4d;
      }

      .edit-link {
        background-color: #45a049;
      }

      .edit-link:hover {
        background-color: #38833c;
      }

      p {
        display: flex;
        align-items: center;
        justify-content: space-between;
      }
      input[type="text"],
      input[type="date"],
      input[type="number"],
      input[type="file"] {
        width: 75%;
        border: #ccc solid 2px;
        margin: 0;
        padding: 0.2rem;
      }
    </style>
  </head>
  <body>
    <div class="ebook-container">
      <form
        action="/ebooks/<%= ebook.ISBN %>"
        method="POST"
        enctype="multipart/form-data"
      >
        <input type="hidden" name="_method" value="PUT" >
        <p>
          <strong>Title:</strong>
          <input
            type="text"
            name="titre"
            value="<%= ebook.titre %>"
            placeholder="Titre"
            required
          />
        </p>
        <p>
          <strong>Description:</strong>
          <input
            type="text"
            name="description"
            value="<%= ebook.description %>"
            placeholder="Description"
            required
          />
        </p>
        <p>
          <strong>Date:</strong>
          <input 
            type="date" 
            name="date_edition" 
            value="<%=ebook.date_edition.toISOString().split("T")[0] %>"
            placeholder="DateEdition" 
            required 
          />
        </p>
        <p>
          <strong>Category:</strong>
          <input
            type="text"
            name="categorie"
            value="<%= ebook.categorie %>"
            placeholder="Categorie"
            required
          />
        </p>
        <p>
          <strong>Price:</strong>
          <input
            type="number"
            name="prix"
            value="<%= ebook.prix %>"
            placeholder="Prix"
            step="0.01"
            required
          />
        </p>
        <p>
          <strong>Ebook Cover:</strong>
          <input type="file" name="photo" placeholder="Photo URL" />
        </p>

        <img src="<%= ebook.photo %>" alt="Ebook Cover" />
        <br />

        <button type="submit" class="edit-link"> Edit </button>
        <a href="/ebooks/" class="back-link">Back to List</a>
      </form>
    </div>
  </body>
</html>
